/**
 * @format
 */

import { AppRegistry } from 'react-native';
import cursos from './src/pages/Cursos';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => cursos);
